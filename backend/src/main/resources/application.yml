spring:
  application:
    name: borrow-buddy
  datasource:
    url: jdbc:postgresql://db.pagwpnnirfljymcxeses.supabase.co:5432/postgres
    username: postgres
    password: "+!Q2vZzsH*6&%Ve"
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
  flyway:
    enabled: true
    baseline-on-migrate: true
  data:
    redis:
      host: localhost
      port: 6379
  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 5MB

jwt:
  secret: your_super_secret_key_which_should_be_long_enough_for_hs256_must_be_at_least_256_bits
  expiration: 86400000 # 1 day

aws:
  s3:
    access-key: dummy
    secret-key: dummy
    bucket: borrowbuddy-assets
    region: us-east-1

firebase:
  config-path: classpath:firebase-service-account.json
